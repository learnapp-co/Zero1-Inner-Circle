{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma db push --accept-data-loss && npm start",
    "healthcheckPath": "/inner-circle/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
